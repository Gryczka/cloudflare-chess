# Copy this file to .dev.vars for local development.
# .dev.vars is gitignored and never committed.

# Signing key for session cookies (QR-1). Generate a value with:
#   openssl rand -hex 32
# Leaving this blank is acceptable only for local development, where the app
# falls back to a known insecure value. Production deploys require a real key.
AUTH_SECRET=
