<div class="background-lines" aria-hidden="true">
	<div class="background-inner">
		<div class="line line--left"></div>
		<div class="line line--right"></div>
	</div>
	<div class="background-outer">
		<svg class="dots" aria-hidden="true">
			<defs>
				<pattern id="dot-pattern" x="0" y="0" width="12" height="12" patternUnits="userSpaceOnUse">
					<circle cx="6" cy="6" r="0.75" fill="var(--cf-border)"></circle>
				</pattern>
			</defs>
			<rect x="0" y="0" width="100%" height="100%" fill="url(#dot-pattern)"></rect>
		</svg>
		<div class="line line--left"></div>
		<div class="line line--right"></div>
	</div>
</div>

<style>
	@keyframes backgroundFadeIn {
		from { opacity: 0; }
		to { opacity: 1; }
	}

	.background-lines {
		position: fixed;
		inset: 0;
		z-index: -10;
		pointer-events: none;
		animation: backgroundFadeIn 2s ease-out 100ms both;
	}

	.background-inner,
	.background-outer {
		position: absolute;
		top: 0;
		left: 50%;
		height: 100%;
		transform: translateX(-50%);
	}

	.background-inner {
		z-index: 1;
		width: 1200px;
		max-width: 100%;
		background: var(--cf-bg-100);
	}

	.background-outer {
		width: 1480px;
		max-width: 100%;
	}

	.dots {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
		opacity: 0.7;
	}

	.line {
		position: absolute;
		top: 0;
		width: 1px;
		height: 100%;
		background-image: linear-gradient(to bottom, var(--cf-border-light) 50%, transparent 50%);
		background-size: 1px 32px;
		background-repeat: repeat-y;
	}

	.line--left { left: 0; }
	.line--right { right: 0; }

	@media (max-width: 767px) {
		.background-lines {
			display: none;
		}
	}
</style>
